type Query {
    bookDetails(bookId:Int) : [Book]
}

type Author {
    id: ID,
    firtName: String
    lastName: String
}


type Book {
    id: ID
    title: String
    pageCount : Int
    author: Author
}